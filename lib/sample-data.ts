// Sample data initialization removed
// System is now ready to work with real backend data via Spring Boot APIs
// All data will be fetched from the backend REST endpoints

export function initializeSampleData() {
  // No sample data initialization - system expects real data from backend
  console.log('[v0] System ready for backend integration. Configure API endpoints in .env');
}
